{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug cemgen generate",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/main.go",
      "args": [
        "generate",
        "${input:generate}"
      ]
    }
  ],
  "inputs": [
    {
      "id": "generate",
      "type": "promptString",
      "description": "Arguments...",
      "default": "../redhat-ux/red-hat-design-system/elements/*/*.ts"
    }
  ]
}
