/**
 * Main entry point for large component library
 *
 * This is a stub file for the benchmark fixture.
 * The actual custom element definitions are declared in the manifest (custom-elements.json).
 *
 * In a real project, this would import and register all components.
 * For testing/benchmarking purposes, we rely on the manifest for type information.
 */

// No actual imports needed for fixture testing
// The LSP uses the manifest to understand what elements are available
export {};
